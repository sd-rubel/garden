// config.js - GitHub repo details (Edit these to match your repo)
window.APP_CONFIG = {
    owner: 'sd-rubel',      // Change to your GitHub username
    repo: 'garden',             // Change to your repository name
    branch: 'main',                     // usually 'main' or 'master'
    jsonFolder: 'json',                 // folder containing .json files
    baseUrl: ''                         // leave empty for root, or set if using project site subpath
};